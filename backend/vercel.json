{
	"version": 2,
	"builds": [
		{
			"src": "api/*.go",
			"use": "@vercel/go"
		}
	],
	"routes": [
		{ "src": "/user", "dest": "/api/user.go" },
		{ "src": "/cover-letter", "dest": "/api/cover_letter.go" },
		{ "src": "/(.*)", "dest": "/api/index.go" }
	]
}
